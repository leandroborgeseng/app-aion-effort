// Stub para prismaService no frontend
// Este arquivo é usado apenas durante o build do frontend para evitar erros
// A função getSectorNamesFromUserSector nunca será executada no frontend

export async function getPrisma() {
  return null;
}

