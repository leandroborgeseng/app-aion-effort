name: CI - Validação e Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  validate:
    name: Validar Código
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📦 Instalar pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: 📥 Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: 🔍 Validar sintaxe TypeScript
        run: pnpm exec tsc --noEmit

      - name: ✅ Validar Prisma Schema
        run: pnpm prisma:generate

  build-backend:
    name: Build Backend
    runs-on: ubuntu-latest
    needs: validate
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📦 Instalar pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: 📥 Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: 🔧 Gerar Prisma Client
        run: pnpm prisma:generate

      - name: 🐳 Build Docker Backend
        run: |
          docker build -f Dockerfile.backend -t app-aion-effort-backend:test .

      - name: ✅ Verificar imagem criada
        run: docker images app-aion-effort-backend:test

  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    needs: validate
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📦 Instalar pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: 📥 Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: 🏗️ Build Frontend
        run: pnpm build

      - name: 🐳 Build Docker Frontend
        run: |
          docker build -f Dockerfile.frontend -t app-aion-effort-frontend:test .

      - name: ✅ Verificar imagem criada
        run: docker images app-aion-effort-frontend:test

  lint-and-format:
    name: Verificar Formatação
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 📦 Instalar pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: 📥 Instalar dependências
        run: pnpm install --frozen-lockfile

      # Nota: Se você adicionar ESLint/Prettier no futuro, descomente:
      # - name: 🔍 Executar ESLint
      #   run: pnpm lint
      # 
      # - name: 🎨 Verificar formatação
      #   run: pnpm format:check

      - name: ✅ Validação básica concluída
        run: echo "Formatação OK (sem linter configurado ainda)"

